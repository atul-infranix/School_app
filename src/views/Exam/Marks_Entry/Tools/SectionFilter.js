import { Select } from 'components/ui'
import React from 'react'

const SectionFilter = (props) => {
  return (
    <Select className="ml-4" size="sm" placeholder="Section"/>
  )
}

export default SectionFilter